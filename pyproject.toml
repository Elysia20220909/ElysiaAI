[project]
name = "elysia-ai-kernel"
version = "0.1.0"
description = "AI Core for ElysiaAI"
dependencies = [
    "fastapi",
    "uvicorn",
    "langchain",
    "langchain-community",
    "ollama",
    "pymilvus[model]",
    "python-dotenv",
    "pydantic"
]

[tool.setuptools]
packages = ["kernel"]
