{
  "name": "elysia-cuda",
  "version": "1.0.0",
  "description": "CUDA GPU acceleration for embedding and similarity search",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "test": "node test.js"
  },
  "dependencies": {
    "ffi-napi": "^4.0.3",
    "ref-napi": "^3.0.3"
  }
}
