{
  "dashboard": {
    "id": null,
    "title": "ElysiaAI Metrics",
    "panels": [
      {
        "type": "graph",
        "title": "Request Rate",
        "targets": [{ "expr": "elysia_requests_total" }],
        "datasource": "Prometheus"
      },
      {
        "type": "graph",
        "title": "Error Rate",
        "targets": [{ "expr": "elysia_errors_total" }],
        "datasource": "Prometheus"
      },
      {
        "type": "graph",
        "title": "Active Connections",
        "targets": [{ "expr": "elysia_active_connections" }],
        "datasource": "Prometheus"
      }
    ]
  }
}
